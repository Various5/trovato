# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual envs
.venv/
venv/
env/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# App data — must NOT be committed
data/
app_data/
backups/
chroma/
cache/
logs/
*.db
*.db-journal
*.sqlite
*.sqlite3

# PDFs and indexed content
documents/
pdfs/
samples/*.pdf

# Secrets
.env
.env.local
*.key
*.pem
secrets/

# Installer / build artifacts
*.spec
!installer/localdoc.spec
installer/build/
installer/dist/
installer/Output/
*.exe.tmp

# Test artifacts
.coverage
.coverage.*
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Logs
*.log

# Local agent / runtime state — machine-specific, never commit
# (.claude/ = Claude Code local config + scheduled_tasks.lock; un-ignore settings.json if you ever want to share it)
.claude/
# NiceGUI per-user session storage (storage-user-*.json)
.nicegui/
# Local screenshots / capture artifacts (force-add a specific one with `git add -f` if needed for docs)
screenshots/
